baseurl: "" # the subpath of your site, e.g. /blog
url: "https://guidepost.design" # the base hostname & protocol for your site, e.g. http://example.com

# Build settings
markdown: kramdown

sass:
  sass_dir: assets/css

# Clock configuration
guidepost_location: "Kathmandu, NP"
guidepost_timezone: "Asia/Kathmandu"
guidepost_lat: "27.678553"
guidepost_lng: "85.325931"

defaults:
  - scope:
      path: "assets/img"
    values:
      image: true
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "base"
      hiddable_header: "false"

collections:
  journal:
    output: true
    permalink: /journal/:path

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - LICENSE.txt
  - README.md
